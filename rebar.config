{deps, [
    {lager, "2.0.0", {git, "https://github.com/basho/lager.git", {tag, "2.0.0rc2"}}}
]}.

{erl_opts, [
    {parse_transform, lager_transform}
]}.

{clean_files, ["deps", "log", "logs", "*.eunit", "ebin", "test/*.beam"]}.
